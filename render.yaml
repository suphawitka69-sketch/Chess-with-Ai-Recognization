services:
  - type: web
    name: chess-analytics-hub
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: flask --app app run --host 0.0.0.0 --port $PORT
    plan: free